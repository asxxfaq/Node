Reusable block of code is modules

1> Core modules

    these are built into node.js these are fs,http,path ,url,os,events


    Fs(file system)

    common methods

    fs.mkdir(create a folder)
    fs.WriteFile(Write a file)
    fs.appendFile(append data)
    fs.readFile(read File)
    fs.unlink(delete file)
    rs.rmdir(remove folder)

2> 



URL


events
allows custom creations